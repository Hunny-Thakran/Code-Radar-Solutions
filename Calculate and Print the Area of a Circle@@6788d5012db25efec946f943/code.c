#include <stdio.h>
#include<math.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    float a,b;
    scanf("%f",&f);
    b=pow(a,2);
    printf("Area: %.2f",3.14*b);
    return 0;
}