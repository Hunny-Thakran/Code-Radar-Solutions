#include <stdio.h>
#include <math.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    float a;
    scanf("%f",&f);
    printf("Area: %.2f",3.14*pow(a,2));
    return 0;
}